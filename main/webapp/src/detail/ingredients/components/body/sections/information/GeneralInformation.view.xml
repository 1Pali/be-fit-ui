<mvc:View
        controllerName="pc.my.be-fit.src.detail.ingredients.components.body.sections.information.GeneralInformation"
        xmlns="sap.m"
        xmlns:mvc="sap.ui.core.mvc"
        xmlns:form="sap.ui.layout.form"
>
    <form:Form
        id="isGeneralInformationForm"
        visible="{ui>/editMode}"
    >
        <form:layout>
            <form:ResponsiveGridLayout
                    labelSpanXL="4"
                    labelSpanL="4"
                    labelSpanM="12"
                    labelSpanS="12"
                    adjustLabelSpan="false"
                    emptySpanXL="0"
                    emptySpanL="0"
                    emptySpanM="0"
                    emptySpanS="0"
                    columnsXL="2"
                    columnsL="2"
                    columnsM="2"
                    singleContainerFullSize="false"
            />
        </form:layout>
        <form:formContainers>
            <form:FormContainer>
                <form:formElements>
                    <form:FormElement
                        label="{i18n>isName}"
                    >
                        <form:fields>
                            <Input
                                id="isName"
                                value="{data>name}"
                                valueState="{= ${data>name} !== '' ? 'None' : 'Error'}"
                                change="onFieldChange"
                                required="true"
                            />
                        </form:fields>
                    </form:FormElement>
                    <form:FormElement
                        label="{i18n>isEnergy}"
                    >
                        <form:fields>
                            <Input
                                id="isEnergy"
                                value="{data>energy}"
                                valueState="{= ${data>energy} !== '' ? 'None' : 'Error'}"
                                change="onFieldChange"
                                type="Number"
                                required="true"
                            />

                        </form:fields>
                    </form:FormElement>
                    <form:FormElement
                        label="{i18n>isProtein}"
                    >
                        <form:fields>
                            <Input
                                id="isProtein"
                                value="{data>protein}"
                                valueState="{= ${data>protein} !== '' ? 'None' : 'Error'}"
                                change="onFieldChange"
                                type="Number"
                                required="true"
                            />
                        </form:fields>
                    </form:FormElement>
                    <form:FormElement
                        label="{i18n>isCarbohydrate}"
                    >
                        <form:fields>
                            <Input
                                id="isCarbohydrate"
                                value="{data>carbohydrate}"
                                valueState="{= ${data>carbohydrate} !== '' ? 'None' : 'Error'}"
                                change="onFieldChange"
                                type="Number"
                                required="true"
                            />
                        </form:fields>
                    </form:FormElement>
                </form:formElements>
            </form:FormContainer>
            <form:FormContainer>
                <form:formElements>
                    <form:FormElement
                        label="{i18n>isFat}"
                    >
                        <form:fields>
                            <Input
                                id="isFat"
                                value="{data>fat}"
                                valueState="{= ${data>fat} !== '' ? 'None' : 'Error'}"
                                change="onFieldChange"
                                type="Number"
                                required="true"
                            />
                        </form:fields>
                    </form:FormElement>
                    <form:FormElement
                        label="{i18n>isFiber}"
                    >
                        <form:fields>
                            <Input
                                id="isFiber"
                                value="{data>fiber}"
                                valueState="{
                                    parts: [
                                        {path: 'data>fiber'},
                                        {value: 'isFiber'}
                                    ],
                                    formatter: '.getStateAndValidate'
                                }"
                                change="onFieldChange"
                                type="Number"
                                required="true"
                            />
                        </form:fields>
                    </form:FormElement>
                    <form:FormElement
                        label="{i18n>isPrice}"
                    >
                        <form:fields>
                            <Input
                                value="{data>price}"
                                type="Number"
                                required="false"
                            />
                        </form:fields>
                    </form:FormElement>
                </form:formElements>
            </form:FormContainer>
        </form:formContainers>
    </form:Form>
</mvc:View>