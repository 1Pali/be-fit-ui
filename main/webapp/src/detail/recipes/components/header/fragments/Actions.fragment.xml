<core:FragmentDefinition
        xmlns="sap.m"
        xmlns:core="sap.ui.core"
>
    <Button
        text="{i18n>EditRecipeButton}"
        tooltip="{i18n>EditRecipeButtonToolTip}"
        visible="{= ${ui>/editMode} === false}"
        type="Emphasized"
        press=".onEditPress"
    />

    <Button
        text="{i18n>RemoveRecipeButton}"
        tooltip="{i18n>RemoveRecipeButtonToolTip}"
        type="Transparent"
        press=".onDeletePress"
    />
</core:FragmentDefinition>
